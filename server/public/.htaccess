<IfModule mod_rewrite.c>
    RewriteEngine On

    # Arahkan semua request ke index.php jika file atau folder yang diminta tidak ada
    RewriteCond %{REQUEST_FILENAME} !-f
    RewriteCond %{REQUEST_FILENAME} !-d
    RewriteRule ^ index.php [QSA,L]
</IfModule>